apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: prod

resources:
  - ../../base

images:
  - name: your-registry/web-app
    newTag: dev-a1b2c3d   # bumped by scripts/promote.sh staging prod, only after staging soak

replicas:
  - name: web-app
    count: 4

patches:
  - path: configmap-patch.yaml
  - path: resource-limits-patch.yaml
