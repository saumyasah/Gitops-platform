apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: staging

resources:
  - ../../base

images:
  - name: your-registry/web-app
    newTag: dev-a1b2c3d   # bumped by scripts/promote.sh dev staging

replicas:
  - name: web-app
    count: 2

patches:
  - path: configmap-patch.yaml
