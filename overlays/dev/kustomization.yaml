apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: dev

resources:
  - ../../base

images:
  - name: your-registry/web-app
    newTag: dev-a1b2c3d

replicas:
  - name: web-app
    count: 1

patches:
  - path: configmap-patch.yaml
